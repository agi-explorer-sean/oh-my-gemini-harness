# context-injector

This feature injects context into agent prompts based on the current session
state.

## File Sources

### Re-exported from oh-my-opencode

-   `index.ts`: Re-exports the context injector from the vendor core.
